route[SET_DIRECTION_FLAG] {
   if (is_in_subnet($si, "CORESUBNET")) {
    xlog("L_INFO", "call comes from core subnet\n");
    setflag(FLT_FROM_CORE);
    return 1;
   }
    xlog("L_INFO", "call comes from public subnet\n");
    setflag(FLT_FROM_PUBLIC);
    return 1;
}